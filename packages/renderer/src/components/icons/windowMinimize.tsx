import type { IconProps } from '.';

export const windowMinimize = (props: IconProps) => (
    <path
        d='
            M8,55 H92
        '
        stroke={props.color}
        strokeWidth={16}
        strokeLinecap='round'
    />
);
